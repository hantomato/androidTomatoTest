<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nmj.androidtomatotest" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".viewpager.ViewPagerTest1Activity" />
        <activity android:name=".viewpager.ViewPagerTest2Activity" />
        <activity android:name=".recyclerview.RecyclerViewTest1Activity" />
        <activity android:name=".recyclerview.RecyclerViewTest2Activity" />
        <activity android:name=".recyclerview.RecyclerViewTest3Activity" />
        <activity android:name=".coordinator.Coordinator1Activity" />
        <activity android:name=".coordinator.Coordinator3Activity" />
        <activity android:name=".coordinator.Coordinator4Activity" android:theme="@style/SecondTheme" />
        <!--<activity android:name=".coordinator.Coordinator3Activity" />-->
    </application>

</manifest>
